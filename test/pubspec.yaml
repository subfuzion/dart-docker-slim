name: dart_docker_test
description: A web server for testing the Docker image for Dart.

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  args: ^1.6.0
  http: ^0.12.2
  shelf: ^0.7.5
  shelf_router: ^0.7.3

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
